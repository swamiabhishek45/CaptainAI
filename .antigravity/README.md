# Antigravity Agent Trace Workspace

This folder is intentionally committed for GDG Cloud Pune APL judging evidence.

Runtime match-strategy traces are emitted as JSON Lines files under `.antigravity/traces/`
when the app has filesystem access. Each trace records agent turns, model selection,
structured recommendations, and the final Gemini Pro decision.
